export type SignUpFormValues = {
  name: string;
  lastName: string;
  email: string;
  password: string;
  termsAndConditions: boolean;
};

export type SignInFormValues = {
  email: string;
  password: string;
};
