generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model books {
  id       Int    @id @default(autoincrement())
  author   String
  title    String
  country  String
  language String
  pages    Int
  year     Int
}
